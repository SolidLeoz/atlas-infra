# Atlas mobile client (voice)

Wake word should be "Atlas" and MQTT config should use the central `.env`.

Audio note:
- Provide a local recording (e.g. `wakeword_atlas_it.wav`) or install a TTS tool to generate it.
- This host currently has no TTS binaries available.
